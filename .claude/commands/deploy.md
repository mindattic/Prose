---
description: Deploy Prose via MindAttic.Deploy (build, sync UiUx, push master, Azure CI/CD build/migrate/deploy).
---

Canonical definition: **`.prose/commands/deploy.md`**. Read it and follow it — this file is a
thin pointer only; the real, maintained runbook lives in `.prose/` so every client shares one copy.
