# quickload

Print the saved portable handoff and consume it by default. Pass `-Keep` when inspecting without
consuming.

Implementation: `tools/prose-agent.ps1 load` or `tools/prose-agent.ps1 load -Keep`.
