# /reader-proxy — alias for /reader-qa

This is an alias of `/reader-qa` — same runbook, same behavior (see `.prose/commands/reader-qa.md`),
just callable under the subsystem's own name. Canonical methodology: `docs/READER-QA.md`.
