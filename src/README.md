# Prose v3

The active engine for the Prose story-generation platform — command-line only (`Prose.Cli` +
`Prose.Mcp`), backed by SQL Server canon.

**Full documentation lives in the root [`README.md`](../README.md)** — architecture, history,
Dynamic Context Memory, the CLI/MCP reference, repository layout, database, tests, deployment,
and everything else. This stub exists only so the directory doesn't look abandoned; it is not a
second copy of that document.
