@* NavMenu — controls consolidated into AppBanner (2026-06-16).
   This component is kept as an empty stub so existing references in
   MainLayout continue to compile. All nav controls, search, inbox polling,
   universe switch, and breadcrumb now live in AppBanner / AutoBreadcrumb. *@
