<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\StreetSamurai.Core\StreetSamurai.Core.csproj" />
    <ProjectReference Include="..\StreetSamurai.Shared\StreetSamurai.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
    <UserSecretsId>mindattic-vault-shared</UserSecretsId>
  </PropertyGroup>

  <!-- Include world data in published output so Azure deployment is self-contained.
       STREETSAMURAI_DATA_ROOT env var overrides this path (set in Azure App Configuration). -->
  <ItemGroup>
    <Content Include="..\..\engine\data\**\*" CopyToPublishDirectory="PreserveNewest">
      <Link>engine\data\%(RecursiveDir)%(Filename)%(Extension)</Link>
    </Content>
  </ItemGroup>

  <!-- Pull latest CBG bundle from MindAttic.Content into wwwroot before each build.
       Idempotent: when MindAttic.Content hasn't changed, this is a no-op rewrite.
       If MindAttic.Content isn't checked out next to this repo, the target is skipped. -->
  <Target Name="SyncMindAtticContent" BeforeTargets="BeforeBuild">
    <PropertyGroup>
      <_ContentSync>$(MSBuildThisFileDirectory)..\..\..\MindAttic.Content\sync\sync-streetsamurai.ps1</_ContentSync>
    </PropertyGroup>
    <Exec Command="powershell -NoProfile -ExecutionPolicy Bypass -File &quot;$(_ContentSync)&quot;"
          Condition="Exists('$(_ContentSync)')"
          ContinueOnError="true" />
  </Target>

</Project>
